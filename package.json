{
  "name": "ai-visual-generator",
  "version": "1.0.0",
  "description": "AI Visual Generator for Netlify",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "node-fetch": "^2.6.9"
  }
}
